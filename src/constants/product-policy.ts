// src/config/productCopy.ts
export const DEFAULT_RETURNS_POLICY = `
We accept returns within 30 days of delivery, provided items are unused,
unwashed, and in their original packaging. Please retain your proof of purchase.
`.trim();

export const DEFAULT_DIMENSIONS_COPY = `
All dimensions are approximate and may vary slightly due to the handmade
nature of the product.
`.trim();
